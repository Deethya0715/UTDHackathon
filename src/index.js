import { registerRootComponent } from 'expo';
import React, { useState } from 'react';
import App from './App';  // Import your App component

registerRootComponent(App);  // Register the root component with Expo
